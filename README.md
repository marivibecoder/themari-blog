# themari.blog

Personal website. Hand-drawn style, vibecoded with Claude.

- `index.html` — the whole site (self-contained: fonts embedded, no build step)

## How to publish changes

Edit `index.html`, commit, push. Vercel redeploys automatically.
